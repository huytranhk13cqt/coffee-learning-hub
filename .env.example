# Database (Docker MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=grammar_user
DB_PASSWORD=change_me
DB_ROOT_PASSWORD=change_me_root
DB_NAME=grammar_learning

# Server
PORT=3001
NODE_ENV=development

# CORS (React dev server)
CORS_ORIGIN=http://localhost:5173
